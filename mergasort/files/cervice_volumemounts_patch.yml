spec:
  template:
    spec:
      containers:
        - name: ms-cervice
          image: ghcr.io/cerverless/mergesort:nolibc
          volumeMounts:
          - name: nfs-vol
            mountPath: /app/jobs_busy
